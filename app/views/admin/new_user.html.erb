

<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">New User</h3>
    <i>Allows user to create new user. once the new user is created, an email is sent to the user? or the admin has to send the email and password to the user?
			<br>
			password is auto-generated, and emailed to the user
		</i>
  </div>
  <div class="panel-body">
		<div class="row">
			<div class="">
			<%= form_for @user, :html => { :class => "form-horizontal" }, :url => url_for(:controller => "admin", :action => "create_user") do |f| %>
					<%= render "shared/error_messages" %>
					<div class="form-group">
						<label class="control-label col-md-1">Name</label>
						<div class="col-md-6">
							<%= f.text_field(:name, :class=> "form-control") %>
						</div>
					</div>

					<div class="form-group">
						<label class="control-label col-md-1">Email</label>
						<div class="col-md-6">
							<%= f.text_field(:email, :class=> "form-control") %>
						</div>
					</div>
		
					<div class="form-group">
						<div class="col-md-offset-1 col-md-1">
							<%= f.submit "Create New User", class: "btn btn-large btn-default" %>	
						</div>
					</div>
				  
				  
				<% end %>
			</div>
		</div>
  </div>
</div>
